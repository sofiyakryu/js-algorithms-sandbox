To run JavaScript code, I use Code Runner Extension in VS Code.
I use shortcut ⌃Control+⌥ Option+N (on macOS) or Control+Alt+N (on PC). 
The code will run and the output will be shown in Output Window.